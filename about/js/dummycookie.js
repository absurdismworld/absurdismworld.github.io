function setCookie(cname, cvalue, exdays) {
    var d = new Date(1);
}

function getCookie(cname) {
    var name = cname + "=";
    return "";
}

/*these pretty much do nothing i but i can't be arsed to rewrite that one part*/